# 本项目全部为 CPU 计算：PLS + 闭式矩估计，无张量运算。
#
# ── 为什么是精确 pin 而不是 `>=` ────────────────────────────────────────────
# 弱信号配置下，内层分组交叉验证的 MSE 曲线近乎平坦（相对差约 1e-3），
# 不同 BLAS 实现之间的浮点差足以改变所选成分数，成分数一变聚合量随之偏移。
# 实测：按原种子在 Apple silicon 上整轮重放，237 个受检量中 181 个不变、
# 56 个偏移，幅度最大 -9.3%（划分虚高倍数置信上限 1.71 → 1.55）。
# 统计判定零翻转，但幅度落在第 2--3 位有效数字，`>=` 给不出可复现的环境。
# 逐项比对见 outputs/A14reprocheck_f1_v18.xlsx（237 行）。
#
# ── 想逐位复现所报数字，请走确定性重放路径 ──────────────────────────────
# 不要依赖内层 CV 重新选成分数——那正是跨平台会翻转的一步。
# checkpoints/<配置名>/model_card.md 与 outputs/A13checkpoint_f1_v18.xlsx
# 已逐配置记下选中的成分数；固定该值后，另一架构上重跑可复现到小数点后第六位
# （实测 c=24 → 0.118215）。
#
# ── 环境标注（如实，不冒充）────────────────────────────────────────────────
# 下列版本 = 2026-08-09 复现核验所用的环境，与 29 张模型卡
# 「训练硬件」字段逐张一致：
#     arm64 (Apple silicon), Darwin 27.0.0, CPython 3.13.5
# ⚠ 产出论文所报数字的那台服务器，其软件环境当时未作精确记录。
#   本文件锁的是【复现环境】，不是【原始产出环境】——两者不可混为一谈。
# ⚠ BLAS 实现：运行记录未捕获其标识，故此处不填写具体后端，以免以本机推断冒充实测。
#   复现时请自行打印并随结果一并报告：
#     python -c "import numpy; numpy.show_config()"
#     python -c "import threadpoolctl; print(threadpoolctl.threadpool_info())"

numpy==2.2.5
pandas==2.3.2
scipy==1.15.3
scikit-learn==1.6.1
openpyxl==3.1.5
matplotlib==3.10.5
